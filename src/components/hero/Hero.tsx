import React from "react";
import { HeroWrapper, InnerWrapper, Landscape } from "./Hero.styled";

const Hero = () => (
  <HeroWrapper>
    <InnerWrapper>
      <Landscape />
    </InnerWrapper>
  </HeroWrapper>
);

export default Hero;
